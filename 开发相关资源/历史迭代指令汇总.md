# 历史迭代指令汇总

> 仅记录用户直接输入到 opencode 文本框的原始内容。
> 不包含 AI 的回复、AI 转发给大模型的指令、工具调用结果、内部操作等。
> 按日期分组（每天一个 `## yyyy-MM-dd` 小节），按时间顺序追加。

## 2026-09-01

- Create or update `AGENTS.md` for this repository. The goal is a compact instruction file that helps future OpenCode sessions avoid mistakes and ramp up quickly. Every line should answer: "Would an agent likely miss this without help?" If not, leave it out.
- 1、修改AGENTS.md文件，设置一个全局规则，你所有回复给我的结论都必需使用中文
  2、修改AGENTS.md文件，设置一个全局规则：每次修改程序之前，自动将要修改的程序备份到backup文件夹（如果没有backup文件夹则自行建立一个），备份的格式为 "文件名.后辍名.yyyyMMdd_HHmmss.bak"
  修改@AGENTS.md 增加一条全局规则：项目使用React-Native 开发，修改程序时注意要符合RN规范。另外，注意App的安卓和苹果的兼容。
  3、修改AGENTS.md文件，设置一个全局规则，"开发相关资源"是开发相关资源，与程序主体无关，不需要关注
  4、修改一下 @AGENTS.md 文件，增加一条全局规则：将每次我给你的命令都记录到 @开发相关资源/历史迭代指令汇总.md
  仅记录用户直接输入到 opencode 文本框的原始内容（不含 AI 的回复、AI 转发给大模型的指令、工具调用结果、内部操作等），**按日期分组**（每天一个 `## yyyy-MM-dd` 小节）按时间顺序追加。

  ★ 设计App Sqlite数据库时，思考一下app本地数据库的设计，以后app会升级多个版本，数据库结构也会跟着进行修改。要在不能影响旧版本用户的情况下进行升级。旧版本用户可能有多种情况（比如我发了十个版本，那要考虑到每个版本数据库要如何更新到最新版本）

  ★ 修改一下 @AGENTS.md 文件，增加一条全局规则：如果一个功能或者对象、数据在不页面展示，在没有特殊要求的情况下，需要保持风格、描述等的一致性，以免造成用户困扰。

  ★ 修改一下 @AGENTS.md 文件，增加一条全局规则：涉及到时区的问题全部以用户手机所在地区的时区为准，特别是显示给到用户看的时间，一定要对上。
- 按照这个文档开始执行
  （文档：AI-DEV-SPEC-Telegraph-Downloader.md —— Telegraph 图片下载器项目规范，包含 MVP 范围、技术栈、架构、数据结构、解析/下载/存储方案、权限、Android API 兼容、错误处理、安全、性能、测试用例、Phase 1~8 开发顺序、AI Agent 执行规则、Definition of Done 等。第一轮要求：只做项目环境审查 + Phase 1 + Phase 2，TypeScript 检查 + Android Debug 编译，最后报告修改文件/完成内容/编译结果/已知问题/下一步建议。）
- 继续
  （推进 Phase 3：图片 Grid 预览 / 缩略图 / 选中状态 / 全选 / 取消全选 / 大图查看；按 spec §33 顺序。）
- 继续
  （推进 Phase 4：单张图片下载 → 保存到 MediaStore → 相册可见（spec §33）。确认 Android 存储方案稳定后再做 Phase 5 批量。）
- 继续
  （推进 Phase 5：批量下载 / 任务队列 / 并发 / 重试 / 跳过已存在 / 进度 / 暂停 / 取消；spec §10/§19/§27。10~500 张图片压力测试。）
- 继续
  （修复 Phase 5 downloadQueue 单元测试的 TypeScript 类型错误，继续完成批量下载功能的验证。）
- 继续
  （推进 Phase 6：本地 SQLite 历史记录；URL/标题/图片数/成功/失败/创建时间/保存目录/最后状态；按 AGENTS.md §4 设计 schema_version 元表 + 多版本迁移；Phase 5 完成的 summary 写入数据库；spec §2.1/§22。）
- 继续
  （推进 Phase 7：Share Intent（浏览器分享 URL 进 App 自动填充）+ 剪贴板自动识别 URL；spec §2.1 输入 / §7 / §33 Phase 7。MainActivity.kt onNewIntent + @react-native-clipboard/clipboard。）
- 继续
  （推进 Phase 8：后台下载 + 通知；spec §20/§21。Foreground Service 保活进程 + 原生通知模块转发 JS 进度 + Android 13+ POST_NOTIFICATIONS 权限 + 前台服务类型。）
- 继续
  （MVP 收尾：spec §39/§41 Release 检查——应用图标（下载箭头+图片元素）、启动图、Release 签名、ProGuard/R8、隐私政策、i18n 终检、Definition of Done 核对。）
- 继续
  （继续调查并修复 Release 构建失败：Hermes 编译器 hermesc.exe 未被 RN Gradle 插件正确定位，.hbc 字节码未生成。定位 detectOSAwareHermesCommand 的解析逻辑。）
- 再检查一下，看看还有没有什么问题，没有问题我就真机跑一下了。
  （真机运行前的全面检查：代码质量、构建、潜在运行时问题、配置一致性。）
- app直接闪退
  （真机运行直接崩溃，排查崩溃原因。）
- 这个链接在tg的app里边打开可以看到图片，但是在浏览器上边打开，包含我的app中打开都看不到图片，检查一下问题
  （某个 Telegraph 链接在 Telegram App 内能看到图片，但浏览器和我们的 App 都看不到，排查图片解析/加载问题。）
- C
  （方案 C：两者都做——A 图片加载失败显示明确错误占位（区分防盗链/网络错误）+ B 下载时探测 URL 是否被防盗链拦截，返回明确错误码 ERR_HOTLINK_BLOCKED。）